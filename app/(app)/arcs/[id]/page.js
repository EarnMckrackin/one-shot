import ArcDetailClient from "./ArcDetailClient";
export const metadata = { title: "Reading Order — One Shot" };
export default function ArcDetailPage({ params }) { return <ArcDetailClient params={params} />; }
